<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.conststar.wall.mapper.MapperUser">

    <select id="getUser" resultType="PojoUser">
        select *
        from `user`
        where token = #{token};
    </select>

    <select id="getUserPublic" resultType="PojoUserPublic">
        select *
        from `user`
        where id = #{id};
    </select>

    <insert id="addUser">
        insert into `user` (email, password, name, last_time, create_time)
        values (#{email}, password(#{password}), #{name}, now(), now());
    </insert>

    <select id="findUser" parameterType="PojoUser" resultType="_boolean">
        select count(id)
        from `user`
        where email = #{email};
    </select>

    <update id="login">
        update `user`
        set `last_time`=now(),
            `token` = #{token}
        where email = #{email}
          and password = password(#{password});
    </update>

    <update id="logout">
        update `user`
        set `last_time`= now(),
            token = null
        where token = #{token};
    </update>
</mapper>